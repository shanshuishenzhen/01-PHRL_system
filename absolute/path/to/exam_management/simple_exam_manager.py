def load_full_question_bank(self):
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
        papers = self.load_papers_from_question_bank()
        if not papers:
            messagebox.showerror("错误", "题库数据加载失败或为空！")
            return
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers_from_question_bank()
    if not papers:
        messagebox.showerror("错误", "题库数据加载失败或为空！")
        return
    try:
        q_bank_path = os.path.join('question_bank_web', 'questions.json')
        # 增加文件存在性检查
        if not os.path.exists(q_bank_path):
            raise FileNotFoundError(f"题库文件未找到: {q_bank_path}")
    except Exception as e:
        print(f"加载题库失败: {str(e)}")
        return []

def import_paper_from_question_bank(self):
    papers = self.load_papers